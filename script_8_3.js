function printTimesTable(num) {
    // Loops ten times
    for (var i = 1; i < 11; i++) {
        // Prints the line with the output being the given num multiplied by i
        console.log(num + " * " + i + " = " + num*i);
    }
}